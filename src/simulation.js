/**
 * Contains all functions required to setup and run a full darwinbots
 * simulation.
 * @module simulation
 **/

var assign = require('object-assign');

/**
 * Creates a new simulation datastructure using the provided configuration
 * object.
 * @param {Object} config - Configuration object which establishes how the
 *                          simulation will run.
 * @return {Object} simulation object - contains error string if a problem
 *                                      occurs, otherwise returns a valid
 *                                      simulation object.
 **/
module.exports.createSimFromConfig = function(config) {
  return {};
};

